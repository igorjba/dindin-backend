const accountExistsMessage = { mensagem: 'A conta informada não existe' };
const validPasswordMessage = { mensagem: 'Senha incorreta' };
const zeroBalanceMessage = { mensagem: 'O saldo deve ser zero para continuar esta operação' };
const enoughBalanceMessage = { mensagem: 'Saldo insuficiente' };
const validAmountMessage = { mensagem: 'O valor deve ser superior a 0' };







module.exports = {
/*     allFieldsFilled,
    getAccount,
    accountExists,
    validPassword,
    zeroBalance,
    enoughBalance,
    validAmount, */
    accountExistsMessage,
    validPasswordMessage,
    zeroBalanceMessage,
    enoughBalanceMessage,
    validAmountMessage
};